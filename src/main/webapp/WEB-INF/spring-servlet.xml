<beans:beans xmlns="http://www.springframework.org/schema/security"
  xmlns:beans="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:mvc="http://www.springframework.org/schema/mvc"
  xmlns:context="http://www.springframework.org/schema/context"  
  xmlns:oauth="http://www.springframework.org/schema/security/oauth2"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                      http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
                      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
                      http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
                      http://www.springframework.org/schema/security/oauth2 http://www.springframework.org/schema/security/spring-security-oauth2-1.0.xsd">

  <!-- Scans within the base package of the application for @Components to configure as beans -->
  <!-- @Controller, @Service, @Configuration, etc. -->
  <context:component-scan base-package="org.example" />
  <!-- Enables the Spring MVC @Controller programming model -->
  <mvc:annotation-driven />
  <!-- And serve static content as well -->
  <mvc:default-servlet-handler />

  <!-- where our food is obtained -->
  <beans:bean id="plateClient" class="org.example.PlateClient">
    <beans:property name="apiurl"  value="http://localhost:8080/hello" />
    <beans:property name="apiurls" value="http://localhost:8080/secure" />
    
    <beans:property name="oauth2RestTemplate">
      <oauth:rest-template resource="foodService" />
    </beans:property>
  </beans:bean>
  
  <http>
    <http-basic />
    <!-- Enable OAuth redirection -->
    <custom-filter after="EXCEPTION_TRANSLATION_FILTER" ref="oauth2ClientFilter" />
  </http>
  
  <oauth:client id="oauth2ClientFilter" />
  
  <oauth:resource id="foodService" type="authorization_code" client-id="plate"
                  access-token-uri="http://localhost:8082/oauth/authorize"
                  user-authorization-uri="http://localhost:8082/oauth/user/authorize"
                  pre-established-redirect-uri="http://localhost:8080/secure"
                  scope="read" />

  <!--define an oauth 2 resource for facebook. according to the facebook docs, the 'client-id' is the App ID, and the 'client-secret' is the App Secret -->
  <oauth:resource id="facebook" type="authorization_code" client-id="233668646673605" client-secret="33b17e044ee6a4fa383f46ec6e28ea1d"
                  authentication-scheme="query" access-token-uri="https://graph.facebook.com/oauth/access_token"
                  user-authorization-uri="https://www.facebook.com/dialog/oauth"
                  token-name="oauth_token" client-authentication-scheme="form" />
                  
  <authentication-manager>
    <authentication-provider>
      <user-service>
        <user name="user" password="user" authorities="ROLE_USER" />
      </user-service>
    </authentication-provider>
  </authentication-manager>

</beans:beans>